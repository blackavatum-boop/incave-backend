# C:\incave\pages\products.py
import streamlit as st
import json

# ตั้งค่าให้หน้านี้แสดงผลแบบกว้าง
st.set_page_config(layout="wide")

# ฟังก์ชันสำหรับโหลดข้อมูลจากไฟล์ JSON (ใช้ cache เพื่อให้โหลดเร็วขึ้น)
@st.cache_data
def load_data(filepath):
    try:
        with open(filepath, 'r', encoding='utf-8') as f:
            data = json.load(f)
        return data
    except FileNotFoundError:
        # กรณีหาไฟล์ไม่เจอ ให้แสดงข้อความแจ้งเตือนและคืนค่าเป็น list ว่าง
        st.error(f"ไม่พบไฟล์ {filepath} กรุณาตรวจสอบว่าสร้างไฟล์ถูกต้อง")
        return []

st.title("✨ สินค้าและบริการของเรา")
st.write("เลือกชมผลิตภัณฑ์และบริการที่ออกแบบมาเพื่อสุขภาพที่ดีของคุณ")
st.markdown("---")

# โหลดข้อมูลจากไฟล์ products.json
all_items = load_data('products.json')

# ตรวจสอบว่ามีข้อมูลหรือไม่ก่อนจะแสดงผล
if not all_items:
    st.warning("ยังไม่มีข้อมูลสินค้าหรือบริการในขณะนี้")
else:
    # วนลูปเพื่อแสดงข้อมูลแต่ละรายการ
    for item in all_items:
        # ใช้ st.container() เพื่อจัดกลุ่มเนื้อหาแต่ละรายการ
        with st.container():
            st.header(item['name'])

            col1, col2 = st.columns([1, 2]) # แบ่งหน้าจอเป็น 2 ส่วน (ซ้าย 1 ส่วน, ขวา 2 ส่วน)

            with col1:
                # แสดงรูปภาพ
                st.image(item['image_url'], use_column_width=True)

            with col2:
                # แสดงรายละเอียด
                st.subheader("📝 รายละเอียด")
                st.write(item['description'])

                # แสดงคุณสมบัติและประโยชน์
                if item.get('benefits'):
                    st.subheader("⭐ คุณสมบัติและประโยชน์")
                    for benefit in item['benefits']:
                        st.markdown(f"- {benefit}")

                # แสดงกลุ่มผู้ใช้ที่เหมาะสม
                if item.get('suitable_for'):
                    st.subheader("👥 เหมาะสำหรับ")
                    for suitable in item['suitable_for']:
                        st.markdown(f"- {suitable}")

                # แสดงข้อควรระวัง
                if item.get('warnings'):
                    st.warning(f"**ข้อควรระวัง:** {' / '.join(item['warnings'])}")

            st.markdown("---") # เพิ่มเส้นคั่นระหว่างแต่ละรายการ